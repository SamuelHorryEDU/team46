- paths for icons need to be manually changed (i still need to fix this)

- I have installed a UI theme for cleaner UI. Should be placed in libraries folder if not placed yet. 

- gui somewhat functions, can tab through the different tabs via navigation bar.

- only HOME, USERS and CATALOGUE have tabs at the moment